{% extends 'partials/navbar2.html.twig' %}

{% block title %}réinitialisez votre mot de passe
{% endblock %}

{% block body %}
	<div class="row m-5 p-5">
		<div class="col">
			<h1>réinitialisez votre mot de passe</h1>

			{{ form_start(resetForm) }}
			{{ form_row(resetForm.plainPassword) }}
			<button class="btn btn-primary">réinitialisez mot de passe</button>
			<a class="btn btn-danger" href="{{path('app_home')}}">Annuler</a>
			{{ form_end(resetForm) }}</div>
	</div>

{% endblock %}
