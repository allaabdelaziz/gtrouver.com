<form method="post" action="{{ path('app_messages_delete', {'id': message.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer ce message ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message.id) }}">
    <button class="btn text-danger"><span class="fa fa-trash-o fa-lg" aria-hidden="true"></span></button>
</form>
