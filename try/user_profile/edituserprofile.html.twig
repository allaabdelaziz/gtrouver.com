{% extends 'base2.html.twig' %}


{% block title %}profile
{% endblock %}


{% block body %}


	<div class="container rounded bg-white mt-5 mb-5">
		<div class="row">
			<div class="col-md-3 border-right">
				<div class="d-flex flex-column align-items-center text-center p-3 py-5"><img class="rounded-circle mt-5" width="150px" src="/images/blank-profile-picture-973460_960_720.webp">
					<span class="text-black-50">{{app.user.email}}</span>
					<span></span>
				</div>
			</div>
			<div class="col-md-5 ">
				<div class="p-3 py-5">
					<div class="d-flex justify-content-between align-items-center mb-3">
						<h4 class="text-right">Profile</h4>
					</div>
					<div class="row mt-3">
						{{ form_start(form) }}
						{{ form_widget(form) }}
						<button class="btn btn-primary profile-button">{{ button_label|default('Save') }}</button>
						{{ form_end(form) }}

					</div>
				</div>
			</div>

		</div>
	</div>
</div></div></div>{% endblock %}
